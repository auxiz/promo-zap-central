
# Server Configuration
PORT=3000                  # Default HTTP port (standard for many Node.js applications)
HOST=0.0.0.0               # Default host (0.0.0.0 binds to all network interfaces)

# WhatsApp Configuration
CHROMIUM_PATH=             # Custom path to Chromium browser executable (optional)

# Logging Configuration
LOG_LEVEL=info             # Logging level (error, warn, info, verbose, debug, silly)

# Frontend URL (for CORS configuration if needed)
FRONTEND_URL=http://localhost:5173  # Default URL for the frontend application
